{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npx docusaurus build",
        "outputDirectory": "build"
      }
    }
  ]
}